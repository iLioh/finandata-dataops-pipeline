# DAG de Prefect

El flow ejecutable vive en `src/finandata/flows/pipeline.py`. El grafo evita
tasks ficticias para almacenes y conserva la cadena inmediata de certificación y
publicación definida para la PoC.

